using Code.Pizza.Core.Domain;

namespace Code.Pizza.Web.Factories
{
    public class AutoMapperFactory
    {
        public static void CreateMaps()
        {
            //Mapper.CreateMap<User, UserModel>()
            //    .ForMember( userGroupModel => userGroupModel.Password, options => options.Ignore() )
            //    .ForMember( userGroupModel => userGroupModel.ConfirmPassword, options => options.Ignore() );

            //Mapper.CreateMap<UserModel, User>()
            //    .ForMember( user => user.Version, options => options.Ignore() )
            //    .ForMember( user => user.CreatedOn, options => options.Ignore() )
            //    .ForMember( user => user.UpdatedOn, options => options.Ignore() )
            //    ;
        }
    }
}
